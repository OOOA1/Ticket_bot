🎫 Telegram Ticket Bot — README
===============================

Бот для выдачи PDF-билетов в Telegram с системой волн и Telegram-админкой.

📁 Структура проекта:
---------------------
bot/
├── bot.py             — основной запуск бота, логика /start
├── admin_panel.py     — админские команды (загрузка, удаление и пр.)
├── database.py        — работа с SQLite (юзеры, билеты)
├── config.py          — настройки токена, путей и wave.txt
├── tickets/           — сюда бот кладёт билеты
├── wave.txt           — файл с временем начала текущей волны
├── users.db           — SQLite база (создаётся автоматически)

🛠 Установка:
-------------
1. pip install pyTelegramBotAPI
2. В `config.py` пропиши:
       BOT_TOKEN = "твой_токен"
       DEFAULT_TICKET_FOLDER = "tickets"
       WAVE_FILE = "wave.txt"
3. Создай папку `tickets`
4. Запусти `python bot.py`

📦 Как загрузить билеты:
-------------------------
1. Сложи `.pdf` билеты в одну папку
2. Сожми в архив `.zip` (без вложенных папок!)
3. В Telegram напиши боту `/upload_zip`
4. Пришли архив — бот сам всё распакует и занесёт в БД

👮 Админ-команды:
------------------
✅ Только для user_id из `admin_panel.py → ADMINS = [...]`

/myid           — покажет твой user_id
/new_wave       — начать новую волну (обнуляет выдачу билетов)
/upload_zip     — бот ждёт архив .zip с билетами
/delete_all     — удалить все загруженные билеты
/list_tickets   — прислать список билетов в виде .txt

🔁 Как работает "волна":
-------------------------
Каждому пользователю разрешается получить **1 билет за волну**.
Время волны хранится в `wave.txt` и меняется через `/new_wave`.

📌 Примечания:
--------------
- Билеты можно только .pdf (можно расширить)
- Файлы в архиве должны быть на верхнем уровне (без папок)
- Если билеты закончились — бот об этом напишет